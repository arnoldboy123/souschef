<div class="pages-header">
  <%= image_tag("myfridge.png", :size => "150x38"  )  %>
</div>

<div class="container m-5">
  <div class="row">
    <div class="col-11" class="fridge">
      <ul>
        <% @fridge_items.each do |item| %>
          <% if item.quantity.to_i > 0 %>
            <div class=" " >
              <li class= "fridge m-3 p-0"><strong><%= item.quantity %></strong> x <%= item.ingredient.name %> </li>
            </div>
          <% end %>
        <% end %>
        </ul>
    </div>
  </div>
</div>

<div class="text-center text-small">
  <%= link_to "Add to fridge", new_fridge_item_path, class: "btn-myplanner text-center mt-5" %>
</div>
